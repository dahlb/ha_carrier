#!/usr/bin/env bash

cd "$(dirname "$0")/.."

if [ ! -x .venv/bin/prek ] || [ ! -x .venv/bin/mypy ]; then
	echo "Missing .venv tooling. Run scripts/setup first." >&2
	exit 1
fi

status=0

.venv/bin/prek run -a || status=$?
.venv/bin/mypy . || status=$?

exit "$status"
